# Incoming research packages

Put `Latin_America_Races_and_Briefings.zip` here (or set `ELECTION_RELEASE_ZIP`).

Do not commit the zip unless a large-file strategy is approved. Checksums belong in
`data/releases/`.
