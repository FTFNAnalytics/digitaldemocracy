# Inherited Atlas 223-column contract reference

This pack uses the exact **20-table / 223-column column-name contract** in `contract/columns.json`, copied from `FTFNAnalytics/digitaldemocracy/docs/phase1/cyprus/contracts/columns.json` as the current inherited research-pack contract reference used by adjacent country packs.

The full explanatory Bosnia-origin contract remains repository provenance at `docs/phase1/cyprus/contracts/Inherited_223_Column_Contract.md`; its Bosnia-specific prose is not Iceland evidence and is therefore not duplicated as if it were an Iceland source. The validator hashes and counts the exact local `columns.json` and requires 20 tables / 223 columns.

No schema migration, database write or live schema claim is made.
