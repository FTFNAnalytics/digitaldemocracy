# Slim land package — Albania Prompt BA

`sources/`, `data/events.jsonl`, and `data/results.jsonl` are omitted. Justin accepted with AL-BA-G01 through AL-BA-G21 left open. Do not invent missing numerics, offices, or successor edges.

`SHA256SUMS` is the full-pack manifest and still lists the omitted paths. `validate.py` expects that full pack root and was not re-run on this slim tree. `validation-report.json` is the full-pack receipt.

The Phase 1 approved classifier stays at `Phase1_approved_tiers.json`. The checked-in `schemas/atlas/tiers/albania.json` is the unapproved Prompt BA draft. `research_coverage_complete` stays false. Every draft row keeps `justin_approved: false`.
