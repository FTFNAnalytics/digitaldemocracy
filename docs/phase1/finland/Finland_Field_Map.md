# Finland → Atlas field map — DRAFT

Pinned main `a0c02d0a36ed95cfb593dd7b085b9567d3b171d3`; unchanged master DDL SHA `1c59e81705d2f6172ca4c0e4aea9b4e390c6fa7606ba305a244756ff7e7af8da` and ledger SHA `e36a15fa7952a1561c17ee663b2544bc43544a7bf1d2e3688ceabf648a8ff4d1`. **223 destination columns / 20 tables**, plus migration-owned schema_migration(version,applied_at) with no research mapping. No DDL or importer written.

Legend: O=`data/research/finland/office-register.json`; G=geographies.json; E=events.json; R=results.json; Q=proceedings.json; S=sources.json; CW=identity-crosswalk.json; U=research-gaps.json in the same directory. T=`schemas/atlas/tiers/finland.json`; V=`Finland_Identity_Vectors.json`; Inventory=`Finland_Input_Inventory.json`. `/i` always indexes the corresponding concrete array. L=`country-package-finland`, N=`cdd-observatory-v1`, candidate release R follows the inventory; where R denotes results the array context is explicit.

All JSON pointers in row evidence refer to hashed original retained inputs. JSON-stat flat `/value/i` is resolved using the preserved ordered dimension tuple. Null/missing markers remain in original_status. Only a reported numeric zero maps to zero status. No guessed dates, shares, seats or successor links. Every unknown source property survives in raw_json plus original retained bytes.

The map's per-field conversion column gives the source locator, conversion and null policy; identity/evidence/validation columns apply alongside the exact Identity Rules and complete vectors. These are implementation specifications, not a claim that SQLite CI ran.

## Source disposition and overlap

- Current register: 2026 Statistics Finland municipality classification + ministry council rules; ÅSUB identifies the 16 Åland councils. Historical municipal admission requires an observed 14z7 total and preserves abolished codes.
- 14z7 / 14z6 / 152l overlap one municipal cycle. 14z6 turnout remains raw context; 152l adds only supported seat claims. No extra events from companion measures.
- 14y4/153i describe 21 county councils in 2022 and 2025. Municipality reporting components do not create county offices.
- 13sw/13t4 national totals describe one Eduskunta office; constituencies are not extra chamber offices. 14gv describes one EP delegation; 14h8 stays retained-only detail.
- Presidential 14db creates one cycle and its supported first/runoff proceedings; valid/invalid total rows are not candidates.
- ÅSUB seat and vote tables join the same event; mainland shifted-year Åland observations are retained-only. Seats-only early cycles are not dropped.
- Source final statistical returns map certified with explicit legal-chronology gaps. No complete legal proceedings history, candidate completeness or coverage_complete=true is asserted.
- Tiers come exclusively from the exact draft T bytes. Production approval is a future gate. Next dates outside the alert window preserve offices and histories.


## dataset_lineage

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| provenance_kind | country_package; newly sourced research, not an empty Europe extract. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| description | New Finland elected-government research: current and source-evidenced historical municipal councils, wellbeing-county councils, Eduskunta, elected President, Åland Lagting and EP delegation. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## dataset_release

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| fingerprint_sha256 | H(Inventory /hash_inputs); SHA256 of compact recursively key-sorted UTF-8 JSON, not ZIP hash. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| hash_inputs_json | C(Inventory /hash_inputs), including all effective source/research/tier hashes, accepted overrides(empty here), adapter/method/schema versions and DDL hashes. Exclude reports, vectors, attempts, execution timestamps and ZIP/manifest. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| adapter_version | atlas-finland-full-register/1 documentary contract; no implementation. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| method_version | atlas-preserve-evidence/1. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| schema_version | atlas-master/1. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| research_snapshot_label | 2026-09-20 research capture; per-source updated values stay in retained bytes. Not an election date. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| upstream_release_id | NULL; no existing public Finland package alias supplied. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| validated_counts_json | Inventory /verified_counts; count authored documents separately from actual future imported rows. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| research_coverage_complete | 0: qualified historic totals and named gaps remain. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C({country_id:finland,pinned_main_commit,source_inventory,coverage_complete:false}); retain original metadata, no invented release acceptance. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## publication_release

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## publication_receipt

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| singleton | 1 only in future successfully staged receipt. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| last_publish_attempt_id | Actual future durable attempt_id, not release identity. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| attempted_lineage_id | L. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| attempted_release_id | R; FK selected publication_release(L,R). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## retained_input

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| input_path | Inventory /hash_inputs/inputs/i/input_path exactly; one row for each source/derived/tier member; no self-referential output manifest input. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| input_kind | tier_classification for tier file; package for source/derived research; future accepted override only as override; diagnostic artifact as artifact. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| sha256 | SHA256(original bytes) from inventory; reject mismatch. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| byte_count | Exact byte length from inventory; integer>=0. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| recovery_locator | ZIP member path plus hash; future durable artifact store must retrieve same bytes. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| payload_json | Exact JSON value if JSON; NULL for CSV/XLSX/HTML/ZIP. Retain binaries intact; never execute embedded code or formulas. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## country

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| country_code | FI. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| name | Suomi; official municipality name variants in fi/sv retained. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| polity_kind | sovereign_country. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| region_id | europe; no exclusions inferred from the alert window. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| coverage_status | partial: current requested body classes covered; historic, legal-status and detailed-result gates remain. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| screening_as_of_label | 2026-09-20. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| notes | C(U); Helsinki has one municipal council with combined responsibilities, no extra county ballot. Åland retains its separate council and Lagting cycle series. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| country_id | Constant finland; FK country.country_id. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C({country_id:finland,pinned_main_commit,coverage_complete:false,scope_gates:U}) | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## geography

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| geography_id | G /i/geography_id exactly: FI, FI-AX, FI-M<three-digit municipality code>, FI-HVA<two-digit county code>. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| name | G /i/name exactly; source orthography retained. Body descriptors are documentary labels, not new official translations. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| parent_geography_id | G /i/parent_geography_id: FI for mainland units/Åland; FI-AX for the 16 Åland municipalities; NULL for FI. No inferred modern county parent for historic municipalities. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| effective_from_label | NULL baseline: first observed snapshot and record admission are not an evidenced geography start date. Preserve all labels in raw_json. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| effective_to_label | NULL baseline: absent from current snapshot is not an exact abolition date. Historic observations remain raw. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| country_id | Constant finland; FK country.country_id. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire G /i); keep geography_type, language, vintage and evidence here, not invented DDL columns. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## research_date

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| date_id | Vector or Identity Rules owner-based date key. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| label | E /i/date/value; O /i/next_election/value. Exact string, not capture date. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| precision | E /i/date/precision = year for source statistical cycles; O /i/next_election/precision = day only where the ministry explicitly supplies a date. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| certainty | E /i/date/certainty called for historical occurrences; O /i/next_election/certainty statutory. This does not independently certify source returns. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| year | Integer first four digits of the exact date/value. Source cycle year is retained separately; no mainland-year alignment for Åland. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| month | Parse ISO month only when precision day or month; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| day | Parse ISO day only for day precision; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| range_start_id | NULL baseline; no range claims authored. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| range_end_id | NULL baseline; no range claims authored. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C({owner_type,owner_id,slot,source_date_object,evidence}); do not reinterpret withheld dates. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## office

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| geography_id | O /i/geography_id; required FK. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| name | O /i/name; statutory source body wording or explicitly documentary body-category label plus original place name. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| office_type | O /i/office_type exactly: municipal_council, wellbeing_county_council, national_parliament, elected_president, european_parliament_delegation, autonomous_parliament. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| office_status | O /i/office_status current/historical exactly. Historical offices remain active records. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| record_state | active including historical offices; no implicit withdrawal. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| state_note | C({review_notes:O /i/review_notes,electoral_mode:O /i/electoral_mode,source_code:O /i/source_code}); do not treat a missing raw property as a separate source. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| registry_qualified | NULL: source roster does not establish future candidacy or legal ballot eligibility. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| next_date_id | Vector next_date_id only where O /i/next_election non-null; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| next_date_resolution | resolved where O next_election exists; unknown where NULL (including Åland). Out-of-window 2029/2030 dates do not remove offices. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| next_history_key | NULL: next-date metadata is not a prospective event. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| country_id | Constant finland; FK country.country_id. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire O /i); electoral_mode, source code/vintage, coverage_complete, next metadata and review notes survive losslessly. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## office_tier_classification

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| tier | T /classifications/i/tier: national→national_context; regional/municipal/other unchanged; unknown→NULL. Never calendar strings. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| review_status | needs_review for all known draft tiers; no approved status inferred from unflagged row. NULL tier would require unknown. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| rationale | T /classifications/i/rationale exact; review categories/evidence retained raw. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| classification_path | schemas/atlas/tiers/finland.json. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| classification_kind | tier_classification. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| classification_sha256 | Inventory /tier_sha256; FK to exact retained_input(L,R,path,kind,hash). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C({origin,source_row,qualifiers,evidence}) containing complete authored row and all unknown fields; original source bytes remain retained_input. No information dropped. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## election_event

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| event_id | E /i/event_id = K(event,[finland,N,HK]); exact vector. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| date_id | V /events/i/date_id from E /i/date object; owner event, slot ballot. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| date_resolution | resolved for every authored date including year-only historical events. Missing day does not make known year unknown. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| event_kind | E /i/event_kind ordinary. Statistical source cycle retained; no invented repeat event or exact decisive polling day. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| selected_history_role | E /i/selected_history_role selected: one normalized representation per observed cycle; presidential first/runoff are proceedings of that cycle. Unverified legal chronology stays an explicit hold. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| electoral_system | NULL baseline; source description retained raw, no inferred seat-allocation formula. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| comparability | NULL; no merger-adjusted historical comparability asserted. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| ballot_basis | valid_votes; aggregate totals and invalid ballots are context, never candidate/party result rows. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| share_unit | percent_0_100 including absent shares. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| legal_outcome | E /i/legal_outcome certified means published official statistical final return, not independent certification by this pack. If later evidence identifies annulment/preliminary status, preserve claims and apply a reviewed correction; do not silently relabel. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| record_state | active; retained partial history is not deleted. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| state_note | C({notes:E /i/notes,source_cycle_year:E /i/source_cycle_year,coverage_complete:E /i/coverage_complete}); preserve source qualifications. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| history_key | E/R /i/history_key exactly; FK (N,office_id,HK). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire E /i); valid_votes,total_seats,notes,source_cycle_year and unknown fields survive here; no invented DDL aggregate columns. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## proceeding

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| id_namespace | N. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | Q /i/office_id; FK office (N,oid). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| history_key | Q /i/history_key; FK event (N,oid,HK). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| proceeding_id | Q /i/proceeding_id = K(proceeding,[N,oid,HK,round,source_round_code]). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| kind | Q /i/kind first_round/runoff; source 14db round 1/2 only when candidate values exist. No 2018 runoff invented. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| sequence_no | Q /i/sequence_no = source integer round 1/2. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| supersedes_id | NULL: a runoff does not invalidate its first round. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| legal_outcome | Q /i/legal_outcome certified under the published statistical-return qualification. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | L. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | R. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire Q /i); evidence and source round context retained. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## source

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| source_id | S /i/source_id = finland-- + K(source,[exact URL,exact request JSON or null]); API table/query is part of identity, not URL alone. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| publisher | S /i/publisher actual official host, not invented person. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| title | S /i/input_path filename as deterministic artifact title; upstream titles remain in retained bytes. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| url | S /i/url exactly; POST query preserved in S /i/request and raw_json; query-aware source IDs prevent collisions. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| checked_as_of_label | S /i/retrieved_on exactly = 2026-09-20. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| evidence_grade | NULL; official origin does not automatically certify a research derivative. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| file_sha256 | S /i/sha256 exact retrieved bytes. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| locator | S /i/input_path; exact JSON pointer, HTML table locator or XLSX sheet/cells in evidence_link. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| data_rights | Literal unknown unless a source-specific licence is accepted; no universal licence inferred from official host. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| country_id | Constant finland; FK country.country_id. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| source_namespace | Constant L; never latest global release alias. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire S /i); query, source update metadata, exact bytes and provenance retained. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## party_mapping

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| country_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| party_namespace | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| mapping_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| source_context | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| election_context | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| original_label | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| original_code | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| mapped_group | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| uncertainty | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| lineage_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| release_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |
| raw_json | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Zero rows; no invented cross-election mapping. |


## result_row

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| result_row_id | R /i/result_row_id; exact scoped result token/vector. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| proceeding_id | R /i/proceeding_id: actual presidential first/runoff FK or NULL for other rows. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| candidate_or_list_label | R /i/name exactly, including source statistical groups. Never infer a person from a total row. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| original_party_label | R /i/party_label: source category label or NULL for presidential candidates. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| original_party_code | R /i/party_code: exact source category code or NULL for presidential candidate; source candidate code remains identity_token. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| party_namespace | NULL baseline. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| party_mapping_id | NULL baseline. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| votes | R /i/votes: direct source scalar. No derived/clamped/normalized vote count. Missing NULL; preserve original claims. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| votes_status | R /i/votes_status: recorded positive; zero exact0; unknown NULL. Preliminary/disputed evidence_status is independently retained. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| share | R /i/share: direct source percentage scalar percent_0_100. ÅSUB seat/vote-only tables have NULL share; do not compute it from votes. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| share_status | R /i/share_status, same missing/zero rule. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| share_unit | percent_0_100. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| seats | R /i/seats: explicit direct seat cell joined by election year/body/source party code. StatFin 152l (municipal), 153i (county), 13t4 (Eduskunta); ÅSUB VA006/VA007. Missing match NULL, never zero. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| seats_status | R /i/seats_status, same missing/zero rule. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Exact source scalar/status; NULL ≠ zero; nonnegative votes/seats, share 0–100. |
| elected_flag | R /i/elected_flag is NULL. Party seats and presidential candidate votes do not fabricate individual elected flags. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| is_substitute | NULL; no substitute appointment projection. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| evidence_status | R /i/evidence_status recorded; a source statistical return is retained with legal/history caveats. Future competing claims must remain explicit and disputed/withheld until reviewed. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| history_key | E/R /i/history_key exactly; FK (N,office_id,HK). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| country_id | Constant finland; FK country.country_id. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire R /i); raw original cells/statuses, table-scoped identity_token, ÅSUB seat/vote aliases and all unknown properties survive. No global party-family mapping. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## record_locator

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| record_key | rec-+H([entity_kind,...PKcomponents]); exact Identity Rules tuples. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| entity_kind | country/geography/office/event/proceeding/result_row/source/input per real target. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| country_id | finland for typed targets except input; input target NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| geography_id | Actual gid only for geography target; all other kinds NULL (including office). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| id_namespace | N only for office/event/proceeding/result_row; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| office_id | oid only for office/event/proceeding/result_row; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| history_key | HK only for event/proceeding/result_row; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| proceeding_id | QID only for proceeding target; NULL for result_row target even when the result has a proceeding FK (sparse-target CHECK). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| result_row_id | rid only for result_row; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| party_namespace | NULL baseline. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| party_mapping_id | NULL baseline. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| source_namespace | L only for source target; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| source_id | sid only for source target; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| input_path | Retained path only for input target; otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| source_row_locator | C({derived_path,derived_pointer,source_evidence}); JSON-stat2 /value/i with full dimension tuple; HTML heading/text locator. Do not fabricate JSON pointers into HTML. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## evidence_link

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| evidence_id | ev-+H([record_key,[finland,L,source_id],[input_path,json_pointer_or_locator],claim_kind]); exact identity tuple. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| record_key | FK to real target locator; no orphan invented target. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| source_country_id | finland. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| source_id | Lookup S by evidence input_path; resolved source must exist. Missing known source→fatal. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| source_locator | C(full evidence object): original path/hash plus json_pointer or locator, with dimensions. Match S by input_path and verify hash. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| claim_kind | register_identity, historical_binding, election_date, source_scalar, disjoint_sum, institutional_mode, next_date or research_gap. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| date_claim_id | Actual research_date FK for date claim, otherwise NULL. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| claim_json | C({original_claims,conversion,scope}); raw cells stay available even when scalar NULL. No invented certification. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| source_namespace | Constant L; never latest global release alias. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## unresolved_evidence

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| unresolved_id | unres-+H([record_key,[source_locator],original_token]); exact Identity Rules tuple. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| record_key | Country rec(country,[finland]) for U /i named gates; optional real office locator for listed office_ids only. No orphan target. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| original_token | U /i/original_token; explicitly authored research-gap token, never a fabricated upstream catalogue ID. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| source_locator | C({input_path:data/research/finland/research-gaps.json,json_pointer:/i}); exact source evidence lives in U /i/evidence. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| reason | U /i/reason; missing sources/bindings do not prove no office or zero votes. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C({origin,source_row,qualifiers,evidence}) containing complete authored row and all unknown fields; original source bytes remain retained_input. No information dropped. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## identity_crosswalk

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| entity_kind | office for supplied CW entries; other entity crosswalks from complete vector source tuples when implemented. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| upstream_namespace | CW /i/upstream_namespace: StatFin/kvaa/14z7/municipality/year. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| upstream_id | CW /i/upstream_id exact full source geography code/year; last three labelled municipality digits bind authored office ID. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| record_key | rec(office,[N,CW /i/target_office_id]); referenced office must exist. No guessed successor target. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| reason | CW /i/reason exact. Modern constituency grouping does not establish historic jurisdiction parentage; no successor edges inferred. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| raw_json | C(entire CW /i); source/current labels and evidence retained; no invented legal merger mapping. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Research row evidence→S by exact input_path/hash; unresolved claims→U, never fabricated FK. | Match source projection and DDL type/null/domain; preserve raw input. |


## ingest_attempt

| Column | Source → conversion / NULL policy | Identity rule | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| attempt_id | Future fresh attempt-UUID each run; no attempt created here. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| operator | Actual future initiator, not invented Justin acceptance. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| script_version | Actual future importer version; not implemented here. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| started_at | Actual future UTC ledger start, durable before staging. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| finished_at | NULL until completion, then actual UTC. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| status | Future started/succeeded/failed per ledger; no succeeded row authored here. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| input_inventory_json | C(actual effective inventory and provenance at future attempt). | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| successful_release_id | R only on reconciled successful publication; NULL on failure. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |
| publication_set_json | NULL while started or failed; on success full lineage/release array, preserving unrelated members. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| row_counts_json | Actual measured execution counts; no documentary count represented as executed. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| error_text | Nonempty actual failure message on failed; NULL for started/succeeded. Do not fabricate. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Not run: durable ledger/publication CI must enforce contract. |
| lineage_id | Constant L; ownership FK to selected Finland publication member. | Full table PK and scoped FK from Identity Rules; stable across unchanged input. | Actual future runtime/selected release set only; no operational row authored. | Recompute exact vector/constant; resolved FK exists in full scoped key, else fail closed. |


## Validation boundary

The read-only package validator checks source hashes/pointers, exact draft tier ID-set equality, documentary vectors, arithmetic reconciliation and 223-column coverage. It does not execute migrations, import data, publish a database or approve tiers. Production must reject unapproved tier bytes and fixture inputs. Unknown source fields are retained even when no typed destination exists.
