# Norway → Atlas field map — DRAFT

Main `a0c02d0a36ed95cfb593dd7b085b9567d3b171d3`; master DDL `1c59e81705d2f6172ca4c0e4aea9b4e390c6fa7606ba305a244756ff7e7af8da`; attempt-ledger DDL `e36a15fa7952a1561c17ee663b2544bc43544a7bf1d2e3688ceabf648a8ff4d1`. **223 destination columns in 20 tables.** Migration-owned schema_migration(version,applied_at) has no research rows. DDL unchanged; no importer.

Legend: O=`data/research/norway/office-register.json`; G=geographies.json; E=events.json; R=results.json; Q=proceedings.json; S=sources.json; CW=identity-crosswalk.json; U=research-gaps.json in that directory. T=`schemas/atlas/tiers/norway.json`; V=`Norway_Identity_Vectors.json`; Inventory=`Norway_Input_Inventory.json`. `/i` indexes that concrete array. N=`cdd-observatory-v1`; L=`country-package-norway`; release R is distinguished from results R by context.

Original source objects carry byte hashes and exact JSON-stat `/value/i` pointers with full dimension coordinates. Missing markers survive in original_status. Unknown properties remain in raw_json and retained original bytes. The per-field table specifies source/conversion/null policy, exact identity contract, evidence/FK and validation. Runtime-only fields are future execution values, not invented research facts.

## Source disposition and overlap

- Current offices: independent SSB municipality/county classifications plus official electoral-body rules. Oslo bystyre once; no separate Oslo fylkesting. Oslo's 15 borough committees and Longyearbyen council have separate official sources and named result-history holds.
- Municipal 01180 (1945–2023) supplies observed cycles; 04813 seats enrich the same cycle, never a second event. Table-wide zero nonparticipation categories remain raw without fabricated list entries.
- County 01181 reporting components produce explicitly derived county party sums. Components are not additional county offices or extra events. 04809 seats enrich the same county/year/party only where supported. Prefix geography and official county-total reconciliation remains open.
- Storting 08092 votes/share and 08219 disjoint sex seat counts describe one national office. Sami 05924/05923 describe one elected assembly; constituency components are not extra offices. Preserve the 2005/2009 district transition and 2025 conflicting code.
- No separate proceeding rows, certified election claims, computed percentages, invented party families or successor edges. Legal outcome unknown; source published-statistics status retained.
- Research gaps, official change claims, methodology, HTML and unmatched raw fields remain retained_input. No source scripts execute. National/Sami expected 2029 dates are metadata only; alert window never filters office/history membership.
- Draft tier file exclusively drives tiers; `national→national_context`. Production acceptance remains a gate. Municipal and regional counts derive from evidenced bodies, never calendar labels.


## dataset_lineage

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| provenance_kind | country_package; newly sourced research, not an empty Europe extract. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| description | New Norway elected-government research: current and source-evidenced historical municipal/county councils, Stortinget, Sámediggi, 15 Oslo borough committees and Longyearbyen local council. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |


## dataset_release

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| fingerprint_sha256 | H(Inventory /hash_inputs); SHA256 of compact recursively key-sorted UTF-8 JSON, not ZIP hash. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| hash_inputs_json | C(Inventory /hash_inputs), including all effective source/research/tier hashes, accepted overrides(empty here), adapter/method/schema versions and DDL hashes. Exclude reports, vectors, attempts, execution timestamps and ZIP/manifest. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| adapter_version | atlas-norway-full-register/1 documentary contract; no implementation. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| method_version | atlas-preserve-evidence/1. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| schema_version | atlas-master/1. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| research_snapshot_label | 2026-09-20 research capture; per-source updated values stay in retained bytes. Not an election date. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| upstream_release_id | NULL; no existing public Norway package alias supplied. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| validated_counts_json | Inventory /verified_counts; count authored documents separately from actual future imported rows. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| research_coverage_complete | 0: qualified historic totals and named gaps remain. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C({country_id:norway,pinned_main_commit,source_inventory,coverage_complete:false}); retain original metadata, no invented release acceptance. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## publication_release

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |


## publication_receipt

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| singleton | 1 only in future successfully staged receipt. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| last_publish_attempt_id | Actual future durable attempt_id, not release identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |
| attempted_lineage_id | L. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |
| attempted_release_id | R; FK selected publication_release(L,R). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |


## retained_input

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| input_path | Inventory /hash_inputs/inputs/i/input_path exactly; one row for each source/derived/tier member; no self-referential output manifest input. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| input_kind | tier_classification for tier file; package for source/derived research; future accepted override only as override; diagnostic artifact as artifact. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| sha256 | SHA256(original bytes) from inventory; reject mismatch. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| byte_count | Exact byte length from inventory; integer>=0. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| recovery_locator | ZIP member path plus hash; future durable artifact store must retrieve same bytes. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| payload_json | Exact JSON value if JSON; NULL for CSV/XLSX/HTML/ZIP. Retain binaries intact; never execute embedded code or formulas. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |


## country

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| country_code | NO. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| name | Norge; original Norwegian and multilingual official names retained. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| polity_kind | sovereign_country. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| region_id | europe; no exclusions inferred from the alert window. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| coverage_status | partial: complete independently checked current requested register plus sourced borough/Longyearbyen bodies; historic/legal/aggregate gates remain. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| screening_as_of_label | 2026-09-20. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| notes | C(U); Oslo has one municipal council with county functions, no separate fylkesting ballot. Norway EP representation is not applicable; no EP office. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| country_id | Constant norway; FK country.country_id. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C({country_id:norway,pinned_main_commit,coverage_complete:false,scope_gates:U}) | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## geography

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| geography_id | G /i/geography_id exactly: NO, NO-M<SSB code>, NO-F<county code>, NO-B<borough code>, NO-LONGYEARBYEN. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| name | G /i/name exactly; source orthography retained. Body descriptors are documentary labels, not new official translations. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| parent_geography_id | G /i/parent_geography_id exactly. Current municipalities under their sourced county code; boroughs under NO-M0301; historical jurisdiction versions under NO without guessed modern boundaries. NULL for country. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| effective_from_label | NULL baseline: first observed snapshot and record admission are not an evidenced geography start date. Preserve all labels in raw_json. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| effective_to_label | NULL baseline: absent from current snapshot is not an exact abolition date. Historic observations remain raw. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| country_id | Constant norway; FK country.country_id. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire G /i); keep geography_type, language, vintage and evidence here, not invented DDL columns. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## research_date

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| date_id | Vector or Identity Rules owner-based date key. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| label | E /i/date/value; O /i/next_election/value. Exact string, not capture date. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| precision | E /i/date/precision year for statistical cycles. O /i/next_election/precision day for the called 2027 municipal/county date; year for expected 2029 Storting/Sami cycle. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| certainty | E /i/date/certainty called for known historical occurrence, not certification. O /i/next_election/certainty called or expected exactly. No fabricated polling day. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| year | Integer first four digits of exact source date/value. Historical source year is retained; no imported source capture date as an election day. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| month | Parse ISO month only when precision day or month; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| day | Parse ISO day only for day precision; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| range_start_id | NULL baseline; no range claims authored. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| range_end_id | NULL baseline; no range claims authored. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C({owner_type,owner_id,slot,source_date_object,evidence}); do not reinterpret withheld dates. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## office

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| geography_id | O /i/geography_id; required FK. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| name | O /i/name; statutory source body wording or explicitly documentary body-category label plus original place name. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| office_type | O /i/office_type: municipal_council, county_council, borough_committee, national_parliament, sami_parliament, special_local_council. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| office_status | O /i/office_status current/historical exactly. Historical offices remain active records. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| record_state | active including historical offices; no implicit withdrawal. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| state_note | C({review_notes:O /i/review_notes,electoral_mode:O /i/electoral_mode,source_code:O /i/source_code}); do not treat a missing raw property as a separate source. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| registry_qualified | NULL: source roster does not establish future candidacy or legal ballot eligibility. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| next_date_id | Vector next_date_id only where O /i/next_election non-null; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| next_date_resolution | resolved if O next_election exists, including year-only expected dates; unknown when NULL (Longyearbyen). Out-of-window 2029 offices remain. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| next_history_key | NULL: next-date metadata is not a prospective event. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| country_id | Constant norway; FK country.country_id. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire O /i); electoral_mode, source code/vintage, coverage_complete, next metadata and review notes survive losslessly. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## office_tier_classification

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| tier | T /classifications/i/tier: national→national_context; regional/municipal/other unchanged; unknown→NULL. Never calendar strings. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| review_status | needs_review for all known draft tiers; no approved status inferred from unflagged row. NULL tier would require unknown. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| rationale | T /classifications/i/rationale exact; review categories/evidence retained raw. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| classification_path | schemas/atlas/tiers/norway.json. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| classification_kind | tier_classification. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| classification_sha256 | Inventory /tier_sha256; FK to exact retained_input(L,R,path,kind,hash). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C({origin,source_row,qualifiers,evidence}) containing complete authored row and all unknown fields; original source bytes remain retained_input. No information dropped. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## election_event

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| event_id | E /i/event_id = K(event,[norway,N,HK]); exact vector. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| date_id | V /events/i/date_id from E /i/date object; owner event, slot ballot. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| date_resolution | resolved for every authored date including year-only historical events. Missing day does not make known year unknown. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| event_kind | E /i/event_kind ordinary. Statistical source cycle retained; no invented repeat event or exact decisive polling day. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| selected_history_role | selected for one normalized observed statistical cycle per office/year; no duplicate event from seats versus votes. No proceedings invented. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| electoral_system | NULL baseline; source description retained raw, no inferred seat-allocation formula. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| comparability | NULL; no merger-adjusted historical comparability asserted. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| ballot_basis | valid_votes for observed party-vote returns; source category scopes and explicitly derived county sums stay in raw_json. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| share_unit | percent_0_100 including absent shares. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| legal_outcome | unknown throughout this pack: official statistical publication does not independently prove legal certification or repeat/annulment chronology. Retain final/preliminary source descriptions in evidence without overclaiming. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| record_state | active; retained partial history is not deleted. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| state_note | C({notes:E /i/notes,source_cycle_year:E /i/source_cycle_year,coverage_complete:E /i/coverage_complete}); preserve source qualifications. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| history_key | E/R /i/history_key exactly; FK (N,office_id,HK). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire E /i); valid_votes,total_seats,notes,source_cycle_year and unknown fields survive here; no invented DDL aggregate columns. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## proceeding

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| id_namespace | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| office_id | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| history_key | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| proceeding_id | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| kind | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| sequence_no | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| supersedes_id | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| legal_outcome | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| lineage_id | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| release_id | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| raw_json | No rows: proceeding protocols/rounds/repeat chronology not acquired. Do not manufacture a proceeding from a statistical year; future supplied proceeding requires a scoped event FK and reviewed deterministic identity. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |


## source

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| source_id | S /i/source_id = norway-- + K(source,[exact URL,exact request JSON or null]); API table/query is part of identity, not URL alone. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| publisher | S /i/publisher actual official host, not invented person. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| title | S /i/input_path filename as deterministic artifact title; upstream titles remain in retained bytes. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| url | S /i/url exactly; POST query preserved in S /i/request and raw_json; query-aware source IDs prevent collisions. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| checked_as_of_label | S /i/accessed_date exactly = 2026-09-20; source publication timestamp remains raw. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| evidence_grade | NULL; official origin does not automatically certify a research derivative. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| file_sha256 | S /i/sha256 exact retrieved bytes. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| locator | S /i/input_path; exact JSON pointer, HTML table locator or XLSX sheet/cells in evidence_link. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| data_rights | Literal unknown unless a source-specific licence is accepted; no universal licence inferred from official host. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| country_id | Constant norway; FK country.country_id. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| source_namespace | Constant L; never latest global release alias. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire S /i); query, source update metadata, exact bytes and provenance retained. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## party_mapping

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| country_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| party_namespace | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| mapping_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| source_context | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| election_context | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| original_label | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| original_code | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| mapped_group | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| uncertainty | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| lineage_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| release_id | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |
| raw_json | Zero rows authored. No accepted cross-election party-family mapping. Keep exact list labels/numbers on result_row; no silent coalition/party harmonization. Future mapping requires scoped source/election and reviewed evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Zero rows here; no invented mapping or legal round. |


## result_row

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| result_row_id | R /i/result_row_id; exact scoped result token/vector. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| proceeding_id | NULL throughout; no round or repeat proceeding supplied. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| candidate_or_list_label | R /i/name exactly, including source statistical groups. Never infer a person from a total row. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| original_party_label | R /i/party_label: exact Norwegian source category label; no global party-family mapping. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| original_party_code | R /i/party_code exact, scoped by source series. Reused codes across series are not automatically the same party. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| party_namespace | NULL baseline. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| party_mapping_id | NULL baseline. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| votes | R /i/votes direct SSB scalar for municipal/national/Sami; explicitly derived sum of disjoint municipal reporting components for county party returns. Each component and source county code retained. No clamping, inferred totals, or invented official certification. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| votes_status | R /i/votes_status: recorded positive; zero exact0; unknown NULL. Preliminary/disputed evidence_status is independently retained. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| share | R /i/share direct national/Sami percentage scalar only. Municipal/county shares NULL; no percentage computed from a derived total. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| share_status | R /i/share_status, same missing/zero rule. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| share_unit | percent_0_100. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| seats | R /i/seats: exact source code/year/party join to 04813 municipal or 04809 county. National 08219 sum female+male at whole-country level. Sami 05923 sum female+male across 13 old districts for 2005 or 7 VAL districts since 2009, never both. Missing component NULL; never infer from votes. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| seats_status | R /i/seats_status, same missing/zero rule. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact source claim or disclosed disjoint sum; NULL ≠ zero; nonnegative counts, share 0–100. Preserve disputed evidence. |
| elected_flag | NULL: party/category seats do not establish named elected individuals. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| is_substitute | NULL; no substitute appointment projection. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| evidence_status | R /i/evidence_status recorded or disputed. The Sami 2025 code 98d zero-vote/positive-seat conflict remains disputed; no substitute code/value silently selected. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| id_namespace | Constant N=cdd-observatory-v1; part of office/event/result identities. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| office_id | O/E/R /i/office_id exactly; FK (N,office_id). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| history_key | E/R /i/history_key exactly; FK (N,office_id,HK). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| country_id | Constant norway; FK country.country_id. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire R /i); complete cell dimensions/statuses, county components, seat aggregation, source category identity and unknown fields survive. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## record_locator

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| record_key | rec-+H([entity_kind,...PKcomponents]); exact Identity Rules tuples. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| entity_kind | country/geography/office/event/proceeding/result_row/source/input per real target. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| country_id | norway for typed targets except input; input target NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| geography_id | Actual gid only for geography target; all other kinds NULL (including office). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| id_namespace | N only for office/event/proceeding/result_row; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| office_id | oid only for office/event/proceeding/result_row; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| history_key | HK only for event/proceeding/result_row; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| proceeding_id | QID only for proceeding target; NULL for result_row target even when the result has a proceeding FK (sparse-target CHECK). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| result_row_id | rid only for result_row; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| party_namespace | NULL baseline. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| party_mapping_id | NULL baseline. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| source_namespace | L only for source target; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| source_id | sid only for source target; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| input_path | Retained path only for input target; otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| source_row_locator | C({derived_path,derived_pointer,source_evidence}); JSON-stat2 /value/i with full dimension tuple; HTML heading/text locator. Do not fabricate JSON pointers into HTML. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |


## evidence_link

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| evidence_id | ev-+H([record_key,[norway,L,source_id],[input_path,json_pointer_or_locator],claim_kind]); exact identity tuple. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| record_key | FK to real target locator; no orphan invented target. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| source_country_id | norway. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| source_id | Lookup S by evidence input_path; resolved source must exist. Missing known source→fatal. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| source_locator | C(full evidence object): original path/hash plus json_pointer or locator, with dimensions. Match S by input_path and verify hash. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| claim_kind | register_identity, historical_binding, election_date, source_scalar, disjoint_sum, institutional_mode, next_date or research_gap. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| date_claim_id | Actual research_date FK for date claim, otherwise NULL. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| claim_json | C({original_claims,conversion,scope}); raw cells stay available even when scalar NULL. No invented certification. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| source_namespace | Constant L; never latest global release alias. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |


## unresolved_evidence

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| unresolved_id | unres-+H([record_key,[source_locator],original_token]); exact Identity Rules tuple. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| record_key | Country rec(country,[norway]) for U /i named gates; optional real office locator for listed office_ids only. No orphan target. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| original_token | U /i/original_token; explicitly authored research-gap token, never a fabricated upstream catalogue ID. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| source_locator | C({input_path:data/research/norway/research-gaps.json,json_pointer:/i}); exact source evidence lives in U /i/evidence. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| reason | U /i/reason; missing sources/bindings do not prove no office or zero votes. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C({origin,source_row,qualifiers,evidence}) containing complete authored row and all unknown fields; original source bytes remain retained_input. No information dropped. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## identity_crosswalk

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| entity_kind | office for supplied CW entries; other entity crosswalks from complete vector source tuples when implemented. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| upstream_namespace | CW /i/upstream_namespace: SSB/01180/Region/year or SSB/01180/Region/register. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| upstream_id | CW /i/upstream_id exact official region code plus source year for cycle crosswalk, or exact code alone for register crosswalk; code suffix u is meaningful and never stripped. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| record_key | rec(office,[N,CW /i/target_office_id]); referenced office must exist. No guessed successor target. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| reason | CW /i/reason exact; alias only under evidenced same-name bijective Klass renumbering. Official reform claims retained separately, not inserted as invented successor links. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| release_id | Candidate R from inventory fingerprint; never an attempt ID or other lineage release. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Exact constant/vector; full resolved FK must exist or fail closed. |
| raw_json | C(entire CW /i); source/current labels and evidence retained; no invented legal merger mapping. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Row evidence→S by exact input_path/hash; unresolved claims→U, not fabricated FKs. | Match source projection, null policy and DDL type/domain; retain raw claim. |


## ingest_attempt

| Column | Source → conversion / NULL policy | Deterministic identity | Evidence / FK | Validation assertion |
| --- | --- | --- | --- | --- |
| attempt_id | Future fresh attempt-UUID each run; no attempt created here. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |
| operator | Actual future initiator, not invented Justin acceptance. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| script_version | Actual future importer version; not implemented here. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| started_at | Actual future UTC ledger start, durable before staging. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| finished_at | NULL until completion, then actual UTC. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| status | Future started/succeeded/failed per ledger; no succeeded row authored here. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| input_inventory_json | C(actual effective inventory and provenance at future attempt). | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| successful_release_id | R only on reconciled successful publication; NULL on failure. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |
| publication_set_json | NULL while started or failed; on success full lineage/release array, preserving unrelated members. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| row_counts_json | Actual measured execution counts; no documentary count represented as executed. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| error_text | Nonempty actual failure message on failed; NULL for started/succeeded. Do not fabricate. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Not run: operational ledger/publication gate. |
| lineage_id | Constant L; ownership FK to selected Norway publication member. | Full scoped PK/FK and deterministic tuple in Identity Rules; exact vectors where instantiated. | Actual future runtime/selected release set only. | Exact constant/vector; full resolved FK must exist or fail closed. |


## Validation boundary

Package integrity/projection checks do not execute SQLite, approve tiers, close research holds or demonstrate publication readiness. Future importer must reject fixture inputs, unapproved tier packs and broken resolved-source references. No low-level source field is discarded merely because the unchanged DDL has no typed column for it.
